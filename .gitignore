# Never commit real router credentials or exported tunnel profiles.
secrets/
private/
*.key
*.pem
*.secret
*.subscription
*.conf
!tests/fixtures/*.conf
.env
.env.*

# Local router snapshots, package bundles and diagnostic reports.
# These may contain credentials and are deliberately kept outside Git.
artifacts/
.impeccable/review/
