# Исправление активации подписки — 2026-09-15

Причина: install-luci-dev.sh устанавливал oum-passwall-source-job, но пропускал вызываемый им oum-passwall-bypass-russia. Подписка разбиралась, затем активация завершалась ошибкой и откатом.

Исправлено:
- Установщик копирует helper и назначает права 755.
- Source job проверяет helper до изменения конфигурации; ошибки helper попадают в журнал.
- Добавлена синтаксическая проверка helper.
- dist/oum-install.sh пересобран; внутри проверены helper, права, команда установки и preflight.

На роутер доставлены только два скрипта. Повторная активация через штатный source job завершилась state=success/code=active. Xray и nftables проверены; встроенная HTTP-проверка прошла. Тема не изменялась. Коммитов нет.

Перед финализацией: сохранять эту зависимость в dev-установке и итоговом payload. Не публиковать подписки или содержимое приватных конфигураций.
Резервная копия до активации:
 /home/static/Documents/Codex/2026-09-14/new-chat/outputs/passwall-source-fix/before.tar.gz
